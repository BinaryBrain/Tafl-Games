

Logic = {
	isLegalMove: function (pawn, dest) {
		console.log("Is it a legal move?")
		var coords = pawn.position
		return
	},

	checkDeath: function (movedPawn) {
		console.log("Looking for kills")
		// Check if the just-moved pawn kills somebody
	},

	checkEnd: function () {
		console.log("Looking for the end of the game")
		// return true if the game is terminated
	}
}
